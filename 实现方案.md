# 爱花型袜版设计系统 实现方案

本文档为「爱花型袜版设计系统」的端到端技术实现方案。系统由 Web 端编辑器、小程序端、后端服务、AI 生图代理和爱花型现有用户/订单服务对接五部分构成，目标是把"AI 生成素材 → 自动填充到袜版四区 → 款式快速延伸 → 一键下单到爱花型"这条链路完全自动化，让设计师在 5 分钟内完成一款袜子从灵感到下单的全过程。

---

## 一、项目概述

### 1.1 业务背景

爱花型是袜业 B2B 平台，现有用户系统和订单系统已稳定运营。但产品端长期缺一套面向"袜版图案设计"的工具——设计师需要在 PS / AI 等通用绘图软件里手动拼接素材，再把成品图发给爱花型客服开单。整个流程慢、易出错、新款迭代速度受限于设计师手速。

本项目的价值是把这条链路压缩为：用户在浏览器里输入一句话 → AI 生成素材 → 系统自动填充到袜头/螺口/袜跟/袜口四个区域 → 用户调整密度/旋转/间距 → 点"款式延伸"批量出 4 个变体 → 选一个下单。新人也能在 30 分钟内出一个可下单的设计稿。

### 1.2 目标用户

* **主力用户**：爱花型平台上的中小袜厂设计师 / 老板。审美在线，但不一定会用专业绘图软件，对效率要求高于对工具复杂度的容忍度。
* **次要用户**：终端消费者（C 端）。他们通过小程序进入，做"私人定制袜"，设计完直接下单。

两类用户共用同一套数据模型与 API，通过登录态区分功能权限和订单走向。

### 1.3 核心目标

| 指标 | 目标 |
|------|------|
| 设计一双袜子的端到端时间（从灵感到下单） | ≤ 5 分钟 |
| AI 生图到自动填充落版的成功率 | ≥ 95% |
| 款式延伸生成 4 张变体的耗时 | ≤ 30 秒 |
| 订单提交至爱花型的同步成功率 | ≥ 99.5% |
| 同款用户复购率（设计后 7 天内再次下单） | ≥ 25% |

---

## 二、需求理解与功能拆解

### 2.1 需求文档解读

原始需求文档明确了五大功能模块：

1. 袜版区域管理（袜头/螺口/袜跟/袜口 四区独立 + 蒙版裁剪）
2. 素材生成与填充（AI 生图 + 自动/手动填充）
3. 设计工具（密度/旋转/间距三参数控制）
4. 款式延伸（基于当前设计衍生 1/2/4 张同款）
5. 订单提交 + 用户系统对接（接爱花型现有服务）

需求中明确写"待确认"的事项有三处：

* 订单提交字段（如码数、材质、配色组合等具体规格字段）
* 小程序端选型（微信 / 支付宝 / 多端框架）
* AI 生图 API 的具体供应商与计费方式

### 2.2 待确认问题清单

下表是开发启动前需要与客户敲定的事项。每一项都给出技术建议作为讨论起点：

| 待确认项 | 技术建议 | 理由 |
|---------|---------|------|
| 小程序平台选型 | Taro / UniApp 多端框架，主推微信小程序 | 爱花型用户主要在微信生态，多端框架未来可加支付宝/抖音；UniApp 学习曲线低且文档完善 |
| AI 生图 API | 主选阿里通义万相（文生图）+ 备用 Stable Diffusion 自托管 | 通义万相国内合规、调用稳、单张成本约 0.05 元；自托管 SD 用于生图量大时降本 |
| 订单字段 | 至少需要：尺码组合、底色、材质、数量、配送地址、备注 | 这些是袜业下单最小必填集；具体值域以爱花型现有订单字段为准 |
| 用户系统对接协议 | 优先 OAuth 2.0；次选爱花型自有 token 协议 | OAuth 是行业标准，便于以后接其他第三方 |
| 单用户每日生图配额 | 免费用户 30 张/日，付费用户 300 张/日 | 控制成本同时保证体验；额度设计可调 |
| 设计稿存储期限 | 已下单订单永久保留；草稿 90 天后清理 | 避免 R2/OSS 成本无限增长 |
| 款式延伸的"同款"判定标准 | 基于原图 prompt 注入风格关键词 + ControlNet 锁定布局 | 保证视觉一致性；纯重新生图风格漂移大 |

### 2.3 隐性需求（需求文档未直接写但实际必须做的）

阅读需求时识别出以下隐性必须项，文档里没写但工程上不能省：

1. **设计稿保存与历史版本** — 用户花 5 分钟做的设计如果不能保存，体验崩溃。需要"草稿箱"和"版本回滚"。
2. **作品集与分享** — 设计师会把得意之作发朋友圈引流。需要"分享卡片"自动生成。
3. **素材库** — 用户上传的素材、AI 生成的素材都要进个人素材库，避免每次重新生成。
4. **支付集成** — 订单到爱花型后还涉及支付。需求未提，但下单链路绕不开。本方案默认支付走爱花型现有支付通道，无需重新建。
5. **审核机制** — AI 生图存在违规风险（涉黄涉政），需要前置内容审核。建议接入阿里云内容安全 API 做生图前 prompt 过滤 + 生图后图像审核。
6. **企业 logo 自定义** — 部分品牌客户需要在袜口或袜跟绣自家 logo。需要支持上传透明 PNG 作为额外图层。

---

## 三、技术架构方案

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    用户终端                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐    │
│  │  Web 编辑器   │  │  微信小程序   │  │  其他端（备）    │    │
│  │  (React)     │  │  (UniApp)    │  │                 │    │
│  └──────┬───────┘  └──────┬───────┘  └────────┬────────┘    │
│         └─────────────────┴───────────────────┘             │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS + JWT
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                    BFF 网关层 (Nginx + Node)                 │
│  · 身份认证 / 鉴权                                            │
│  · 请求路由 / 限流                                            │
│  · 静态资源 CDN                                              │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────┐
│                    业务服务层 (Python FastAPI)                │
│  ┌─────────┐ ┌──────────┐ ┌─────────┐ ┌────────┐ ┌────────┐ │
│  │设计稿服务│ │素材库服务 │ │AI生图服务│ │订单服务 │ │用户服务│ │
│  └─────────┘ └──────────┘ └─────────┘ └────────┘ └────────┘ │
└──┬─────────────┬─────────────┬───────────┬───────────┬──────┘
   │             │             │           │           │
   ↓             ↓             ↓           ↓           ↓
┌──────┐  ┌────────────┐ ┌──────────┐ ┌────────┐ ┌──────────┐
│MySQL │  │OSS / R2    │ │AI 生图   │ │爱花型   │ │爱花型    │
│设计稿│  │素材文件    │ │第三方API│ │订单系统 │ │用户系统  │
└──────┘  └────────────┘ └──────────┘ └────────┘ └──────────┘
```

### 3.2 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| Web 编辑器 | React 18 + TypeScript + Vite | 生态成熟，Canvas/Konva 适合做袜版画布 |
| 画布渲染 | Konva.js | 支持图层、蒙版、拖拽缩放、变换；性能好于纯 SVG |
| 状态管理 | Zustand | 轻量，比 Redux 适合中等规模交互 |
| 小程序 | UniApp + Vue3 | 一套代码编译多端，主出微信小程序，Web 同源同构 |
| BFF 网关 | Nginx 反代 + Node Express（中间件层） | Nginx 处理静态资源 + SSL；Node 做认证转发 |
| 业务服务 | Python 3.11 + FastAPI | 类型注解 + 异步并发，与 AI/CV 生态契合 |
| 数据库 | MySQL 8.0 | 事务安全，与爱花型现有数据库技术栈一致 |
| 对象存储 | 阿里云 OSS（主）/ Cloudflare R2（备） | 国内主用 OSS，海外节点用 R2 加速 |
| 缓存 | Redis 7 | AI 生图任务队列、用户 session、热点数据缓存 |
| 任务队列 | Celery + Redis | AI 生图、款式延伸属于长任务，必须异步化 |
| 内容审核 | 阿里云内容安全 | 国内 ICP 合规要求；prompt 文本审核 + 图像审核 |
| 监控 | Prometheus + Grafana | 标配，覆盖响应时间/错误率/AI 调用成本 |

### 3.3 部署架构

```
                         ┌─────────────────┐
                         │   CDN / 域名     │
                         │ aihuaxing-design│
                         └────────┬────────┘
                                  ↓
              ┌───────────────────┼───────────────────┐
              ↓                   ↓                   ↓
       ┌──────────┐         ┌──────────┐        ┌──────────┐
       │  Nginx   │         │  Nginx   │        │  Nginx   │
       │  (主)    │         │  (备 1)  │        │  (备 2)  │
       └──────┬───┘         └──────┬───┘        └──────┬───┘
              └─────────────────┬──┴───────────────────┘
                                ↓
                     ┌──────────────────┐
                     │  K8s 业务集群     │  (3 master + 6 worker)
                     │  · 设计稿服务      │
                     │  · AI 生图代理     │
                     │  · 素材库服务      │
                     │  · 订单服务        │
                     │  · 用户服务        │
                     │  · Celery worker   │
                     └─────────┬────────┘
                               ↓
        ┌──────────┬──────────┼─────────┬──────────┐
        ↓          ↓          ↓         ↓          ↓
   MySQL     Redis       OSS       内容审核   AI 生图 API
   主从架构   集群        华东/南区  阿里云     通义万相 / SD
```

K8s 部署优于直接 Docker Compose 的理由：AI 生图任务是 IO + GPU 密集型混合负载，需要弹性伸缩；K8s 的 HPA 能根据 Celery 队列长度自动扩展 worker 数量。

---

## 四、核心功能实现

### 4.1 袜版编辑器：四区独立 + 蒙版

袜版分四个区域：袜头（toe）、螺口（cuff）、袜跟（heel）、袜口（welt）。每个区域是 Konva 画布上的一个 `Group`，包含：

* **背景层**：纯色或渐变填充
* **蒙版层**：定义该区域的轮廓（PNG alpha 通道）
* **素材层**：用户拖入或 AI 生成的图案，受蒙版裁剪

```
SockEditor (Stage)
├── ToeRegion (Group)
│   ├── Background (Rect)
│   ├── Mask (Image, alpha)
│   └── PatternLayer (Group of Image/Path)
├── CuffRegion (Group)
│   └── ...
├── HeelRegion (Group)
│   └── ...
└── WeltRegion (Group)
    └── ...
```

蒙版采用 PNG alpha 通道而非矢量路径，原因：袜子的实际形状有微弱的不规则曲线（弹性面料的拉伸感），矢量路径表达起来要么不准要么节点过多。PNG alpha 通道由设计师在 PS 里画好后预制，工程上只需要"用这张 PNG 当 mask"。

每个区域内部都有自己的坐标系，`(0,0)` 在区域左上角。这样素材在区域内拖动时不需要考虑全局坐标，简化了逻辑。

### 4.2 素材生成与填充

#### 4.2.1 AI 生图调用链路

```
用户输入 prompt
   ↓
前端 → 业务服务（POST /v1/generate）
   ↓
内容审核（prompt 文本）
   ↓ (通过)
入 Celery 队列
   ↓
worker → 阿里通义万相 API
   ↓
生成的图 → 内容审核（图像）
   ↓ (通过)
图 → OSS（永久保存为该用户素材库一项）
   ↓
返回前端：{ asset_id, url, thumbnail_url }
   ↓
前端：默认自动填充到当前选中区域
```

整条链路平均耗时 8-15 秒（取决于 AI 服务排队情况）。前端用 SSE（Server-Sent Events）实时回传进度，避免用户面对长时间 spinner。

#### 4.2.2 自动填充算法

AI 生成的图通常是 1024×1024 的方图。要把它填充到袜版四区，有三种策略：

| 策略 | 适用场景 | 实现 |
|------|---------|------|
| **平铺重复** | 小图案（碎花、几何） | 把图缩到 100-200px 后在区域内 tile |
| **拉伸适配** | 大图案（一朵完整的花） | 等比缩放到刚好覆盖区域的最长边 |
| **智能分块** | 设计师特别上传的"主图 + 配套小图" | 主图放袜头，小图填螺口 |

默认策略由用户在 prompt 里隐含选择：prompt 中含"图案/纹理/碎花"等关键词 → 平铺；含"主视觉/大图/IP"等 → 拉伸；上传多张 → 分块。系统在生图响应阶段自动判定，用户可在右侧面板手动切换。

#### 4.2.3 手动填充（拖拽）

用户从左侧素材库拖拽图片到袜版任意区域。技术实现：

* 拖拽源：素材库的 `<img>`，使用 HTML5 Drag and Drop API
* 拖拽目标：Konva 画布的四个区域 `Group`，监听 `dragover` 和 `drop`
* 落点判定：拖拽结束时根据鼠标坐标找到最近的区域 Group
* 视觉反馈：拖拽进入区域时该区域闪烁高亮（边框变粉）

落入区域后默认按"拉伸适配"策略填充，用户可立即在右侧面板切换为平铺。

### 4.3 设计工具：密度 / 旋转 / 间距

三个参数对"平铺重复"策略起作用，对"拉伸适配"则部分起作用。

#### 4.3.1 密度（Density）

定义：单位面积内素材出现的次数。技术上等价于"tile 时素材的缩放比例"。

* 最小值：1×（素材原始大小）
* 最大值：8×（素材缩到原始大小的 1/8，单位面积出现 64 个）
* 默认值：3×

UI 上是一个滑块，0–100 映射到 1× ~ 8×。

#### 4.3.2 旋转（Rotation）

定义：每个 tile 的旋转角度。两种模式：

* **统一旋转**：所有 tile 旋转同一角度（0°–360°）
* **随机旋转**：每个 tile 在 ±45° 内随机扰动，营造手绘感

UI 上是一个角度盘 + 一个"随机化"checkbox。

#### 4.3.3 间距（Spacing）

定义：相邻 tile 之间的最小间距像素。

* 最小值：0（无缝紧贴）
* 最大值：50px
* 默认值：5px

间距会把 tile 中心点之间的距离扩大，导致单位面积内 tile 数量减少。这与密度参数有耦合关系，UI 上需要明确告诉用户"密度 + 间距共同决定最终分布"。

#### 4.3.4 实时预览

三个参数任一变化都触发画布重新渲染。性能上需注意：用 Konva 的 `cache()` 缓存渲染结果，参数停止变化 200ms 后才真正重绘，避免拖滑块卡顿。

### 4.4 款式延伸

#### 4.4.1 同款多版本生成机制

用户点"款式延伸"按钮 → 后端拿到当前设计的：

1. 原始 AI prompt（用户最初输入）
2. 当前画布的快照 PNG
3. 内置"同款风格"提示词模板

后端用以下方式生成 N 张衍生款（N ∈ {1, 2, 4}）：

```
new_prompt = original_prompt
           + ", same style and color palette"
           + ", different layout variation"
           + ", high quality fashion sock pattern"

# 用 ControlNet 的 reference_only 锁定颜色与质感
# 用 prompt 重新生成布局
```

ControlNet 的 reference_only 能保证新生成的图与原图色调、光照、笔触一致，但布局上有变化——这恰好是"同款"的视觉定义。

#### 4.4.2 用户体验

* 点击按钮后，弹出选择 1/2/4 张的模态框
* 确认后任务进入 Celery 队列，用户看到一个"正在生成 4 张同款款式..."的进度条
* 完成后在画布右侧出现一个小 gallery，4 张图并排
* 用户点击其中一张 → 当前画布替换为该款式 → 用户可继续编辑或再次延伸

#### 4.4.3 性能与成本

生成 4 张图意味着 4 次 AI 调用，约 30 秒、0.2 元成本。需要的工程优化：

* 4 次调用并发发起（不串行）
* 用 CDN 缓存生成结果，避免用户多次点同样按钮重复生图
* 单用户单设计的款式延伸次数限制为 5 次/天，防滥用

### 4.5 订单提交流程

订单提交是把"画布上的设计 + 用户选择的规格"打包发送到爱花型订单系统。具体步骤：

1. 用户点"提交订单"按钮 → 弹出订单参数表单
2. 表单字段（待客户确认，下表是工程默认）：

```
┌────────────────────────────┐
│ 订单参数                    │
├────────────────────────────┤
│ 尺码：                      │
│   □ 35-37  □ 38-40  □ 41-43│
│   □ 自定义：____________    │
│                            │
│ 数量：[ 100 ] 双           │
│                            │
│ 底色：                      │
│   ○ 白底  ○ 黑底  ○ 自定义 │
│                            │
│ 材质：                      │
│   ○ 棉  ○ 莫代尔  ○ 锦纶   │
│                            │
│ 配送地址：                  │
│   [从历史地址中选择 ▼]      │
│   或新增地址                │
│                            │
│ 备注：                      │
│   [_____________________]  │
│                            │
│  [ 取消 ]  [ 提交订单 ]    │
└────────────────────────────┘
```

3. 用户确认后，前端 → 后端 `POST /v1/orders`
4. 后端把画布栅格化为高清 PNG（300 DPI），存 OSS
5. 后端调用爱花型订单 API：

```
POST https://api.aihuaxing.com/order/create
Headers:
  Authorization: Bearer <access_token>
  Content-Type: application/json
Body:
  {
    "design_image_url": "https://oss.../design_xxx.png",
    "design_thumbnail_url": "https://oss.../thumb_xxx.png",
    "user_id": "<aihuaxing_user_id>",
    "size_range": "38-40",
    "quantity": 100,
    "base_color": "#ffffff",
    "material": "cotton",
    "shipping_address_id": "addr_xxx",
    "note": "...",
    "source": "aihuaxing-design-system",
    "design_metadata": { ... }   // 设计稿原始 JSON
  }
Response:
  {
    "order_id": "...",
    "status": "pending_payment",
    "payment_url": "https://...",
    "estimated_delivery": "..."
  }
```

6. 后端把 `order_id` 与设计稿绑定写本地数据库（用于售后查询）
7. 前端跳转支付页（爱花型现有支付页面）

### 4.6 用户系统对接

#### 4.6.1 注册登录

* **新用户注册**：用户在本系统注册 → 后端调爱花型注册 API → 同步建立爱花型用户 → 返回统一 token
* **老用户登录**：用户输入爱花型账号 → 后端调爱花型登录 API → 校验通过返回 token
* **微信扫码登录**：通过微信开放平台 → 拿 openid → 调爱花型 OAuth → 返回 token

#### 4.6.2 Session 管理

* token 用 JWT，携带 `aihuaxing_user_id` 和过期时间
* 前端存 `localStorage`（Web）或 `wx.setStorage`（小程序）
* 每次接口请求带 `Authorization: Bearer <jwt>`
* 后端中间件统一验证 + 注入 `request.user` 上下文

#### 4.6.3 权限模型

| 角色 | 权限 |
|------|------|
| 游客 | 浏览首页、查看公开作品集 |
| 普通用户 | + 30 张/日生图、设计、下单 |
| 付费用户 | + 300 张/日生图、款式延伸、私有素材库 |
| 厂商账号 | + 批量下单、企业 logo 上传、自定义袜版尺寸 |

---

## 五、数据模型

### 5.1 核心实体

```
User
├── id (PK)
├── aihuaxing_user_id  (外部主键，对接爱花型用户系统)
├── nickname
├── avatar_url
├── tier  (free | paid | enterprise)
├── daily_quota_used
├── daily_quota_reset_at
└── created_at

Design
├── id (PK)
├── user_id (FK)
├── title
├── canvas_json  (Konva 序列化的画布状态)
├── thumbnail_url
├── final_image_url
├── status  (draft | published | ordered)
├── parent_design_id  (款式延伸的来源)
├── original_prompt
├── created_at
└── updated_at

Asset
├── id (PK)
├── user_id (FK)
├── source  (ai_generated | user_uploaded | system_library)
├── url
├── thumbnail_url
├── metadata  (生图 prompt、尺寸、tag)
├── used_count
└── created_at

GenerationTask
├── id (PK)
├── user_id (FK)
├── type  (single | extension)
├── prompt
├── status  (pending | processing | done | failed)
├── result_asset_ids  (生成出的资产列表)
├── error_message
└── created_at

Order
├── id (PK)
├── design_id (FK)
├── user_id (FK)
├── aihuaxing_order_id  (爱花型订单系统返回)
├── size_range
├── quantity
├── base_color
├── material
├── shipping_address
├── status  (pending_payment | paid | shipped | delivered | cancelled)
├── total_amount
└── created_at
```

### 5.2 关键索引

* `User.aihuaxing_user_id` UNIQUE
* `Design.user_id, updated_at DESC`（个人作品列表）
* `Design.status, created_at DESC`（公开作品瀑布流）
* `Asset.user_id, created_at DESC`（个人素材库）
* `Order.aihuaxing_order_id` UNIQUE
* `GenerationTask.status, created_at`（队列扫描）

### 5.3 Canvas JSON 结构

每个设计稿的 `canvas_json` 字段记录画布全部状态，用于历史回溯：

```json
{
  "version": "1.0",
  "stage": { "width": 800, "height": 600 },
  "regions": {
    "toe": {
      "background": "#ffd5e1",
      "mask_url": "https://oss.../mask_toe.png",
      "patterns": [
        {
          "asset_id": "ast_xxx",
          "fill_strategy": "tile",
          "density": 3.5,
          "rotation": { "mode": "uniform", "angle": 0 },
          "spacing": 8
        }
      ]
    },
    "cuff": { ... },
    "heel": { ... },
    "welt": { ... }
  },
  "metadata": {
    "original_prompt": "粉色小花碎花图案",
    "ai_provider": "tongyi-wanxiang"
  }
}
```

`canvas_json` 是产品的核心资产，每次保存都创建新 row（不覆盖旧版本），保留版本号 + 时间戳，做历史回滚用。

---

## 六、接口设计

### 6.1 核心 RESTful 接口列表

```
认证
POST   /v1/auth/register          注册（含同步爱花型）
POST   /v1/auth/login             登录
POST   /v1/auth/refresh           刷新 token
POST   /v1/auth/wechat            微信扫码登录

设计稿
POST   /v1/designs                创建设计稿（自动保存草稿）
GET    /v1/designs                列出当前用户的设计稿
GET    /v1/designs/{id}           读取单个设计稿（含 canvas_json）
PUT    /v1/designs/{id}           更新设计稿
DELETE /v1/designs/{id}           删除设计稿
POST   /v1/designs/{id}/duplicate 复制为新草稿

素材
GET    /v1/assets                 列出素材（含 AI 生成 + 用户上传）
POST   /v1/assets/upload          上传图片素材
DELETE /v1/assets/{id}            删除素材

AI 生图
POST   /v1/generate               单张生图（异步）
POST   /v1/generate/extension     款式延伸（异步，N 张）
GET    /v1/generate/tasks/{id}    查询生图任务状态
GET    /v1/generate/tasks         列出当前用户的生图历史

订单
POST   /v1/orders                 提交订单到爱花型
GET    /v1/orders                 列出当前用户的订单
GET    /v1/orders/{id}            订单详情
GET    /v1/orders/{id}/payment    获取支付链接（透传爱花型）

用户
GET    /v1/users/me               当前用户信息
PUT    /v1/users/me               修改昵称/头像等
GET    /v1/users/me/quota         查询当日剩余配额
```

### 6.2 SSE 推送通道

为避免前端轮询，AI 生图进度走 SSE：

```
GET /v1/generate/tasks/{id}/stream
Content-Type: text/event-stream

data: {"status":"queued","position":3}

data: {"status":"processing","progress":0.2}

data: {"status":"processing","progress":0.7}

data: {"status":"done","asset_id":"ast_xxx","url":"https://..."}
```

### 6.3 错误码规范

```
2xx  成功
4xx  客户端错误
  400 请求参数错误
  401 未登录
  403 无权限或额度耗尽
  404 资源不存在
  409 状态冲突（如已下单设计禁止再编辑）
  429 限流
5xx  服务端错误
  500 通用错误
  502 外部服务（爱花型 / AI API）异常
  503 服务暂不可用
```

业务错误用 `error_code` 字段细化：

```json
{
  "error_code": "QUOTA_EXCEEDED",
  "message": "今日生图配额已用完，请明日再来或升级会员",
  "detail": { "used": 30, "quota": 30, "reset_at": "2026-04-26T00:00:00Z" }
}
```

---

## 七、UI 设计原则

### 7.1 设计语言

整体借鉴现代 SaaS 风格的"克制美学"：浅灰底色 + 白色卡片 + 单一品牌色（玫粉色 `#d4376b`，呼应袜业的女性消费者属性）。**避免**把界面做成"花花绿绿的设计软件"——画布本身已经够花了，UI 框架要尽可能让位给画布。

主色板：

| 用途 | 颜色 |
|------|------|
| 页面底色 | `#eef0f3` |
| 卡片白 | `#ffffff` |
| 卡片半透明 | `rgba(255,255,255,0.86)` |
| 主品牌色 | `#d4376b` |
| 品牌色 hover | `#bf2e5e` |
| 文字主色 | `#13151a` |
| 文字次色 | `#555d6c` |
| 边框 | `rgba(16,18,24,0.09)` |

字体栈：

```
Inter, -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif
```

字号：12px / 13px（基础）/ 14px / 15px / 18px / 24px

### 7.2 主编辑器布局

```
┌─────────────────────────────────────────────────────────────────────┐
│ Logo  设计稿名称                              用户头像  下单 ↗      │  顶栏
├──────┬────────────────────────────────────────┬─────────────────────┤
│      │                                        │                     │
│ 素材 │                                        │  右侧参数面板        │
│ 库   │           袜版主画布                    │                     │
│      │                                        │  · 当前选中区域      │
│ AI   │   ┌──────────┐                         │  · 填充策略          │
│ 生图 │   │   袜口    │                         │  · 密度 [─────●──]   │
│      │   ├──────────┤                         │  · 旋转 [角度盘]     │
│ 我的 │   │   袜跟    │                         │  · 间距 [────●─]     │
│ 设计 │   ├──────────┤                         │  · 颜色              │
│      │   │   螺口    │                         │                     │
│      │   ├──────────┤                         │                     │
│      │   │   袜头    │                         │  ┌─────────────┐    │
│      │   └──────────┘                         │  │ 款式延伸 ✦  │    │
│      │                                        │  └─────────────┘    │
│      │   [撤销] [重做] [保存]                  │                     │
│      │                                        │                     │
└──────┴────────────────────────────────────────┴─────────────────────┘
 90px              flex                            320px
```

* 顶栏 56px 高，浅灰背景，仅放最关键操作。
* 左侧导航 90px 窄列，纯图标 + 悬停展开二级菜单。
* 中央画布 flex 占主，浅灰背景上浮一个带阴影的白色画布。
* 右侧参数面板 320px 固定宽，根据"当前选中区域"动态切换内容。

### 7.3 关键交互

* 选中某区域时，该区域边框变为品牌色描边 + 轻微脉冲动效（提示用户"参数面板正在编辑这个区域"）
* 拖拽素材进入区域时，目标区域边框闪烁
* AI 生图进度条用品牌色，进度满后图片从右侧滑入素材库
* 款式延伸完成后，4 张图卡片以 staggered fade-in 进场（间隔 80ms）
* 鼠标悬停按钮 0.15 秒过渡变深
* 所有 modal 用 `backdrop-filter: blur(2px)` + 半透明黑遮罩

### 7.4 无障碍与响应式

* 所有交互元素键盘可达（Tab + Enter）
* 颜色对比度满足 WCAG AA（4.5:1）
* 主编辑器最低支持 1280×720 桌面分辨率
* 移动端（小程序 / 浏览器移动版）作为只读预览 + 下单流程，编辑功能限制为"调整密度/旋转/间距"，不支持复杂拖拽

---

## 八、开发计划

### 8.1 总周期：12 周

按里程碑切分四个阶段：

| 阶段 | 周次 | 核心交付 |
|------|------|---------|
| M1 基础设施 | W1-W2 | K8s 集群部署 + CI/CD + 用户系统对接 |
| M2 编辑器 MVP | W3-W6 | Web 编辑器主流程（区域 + 蒙版 + 拖拽 + 参数）|
| M3 AI 与延伸 | W7-W9 | AI 生图代理 + 自动填充 + 款式延伸 |
| M4 订单与小程序 | W10-W12 | 订单提交 + 小程序端 + 灰度上线 |

### 8.2 详细工作分解

**M1 基础设施（W1-W2）**

* 域名 + SSL + CDN
* K8s 集群（阿里云 ACK 或自建）
* MySQL 主从 + Redis 集群 + OSS bucket
* CI/CD（GitLab Pipeline 或 GitHub Actions）
* 后端服务骨架（FastAPI + 路由 + 中间件 + 数据库迁移）
* 用户系统对接（注册 / 登录 / 微信扫码 三接口跑通）
* 监控基线（Prometheus + Grafana 起来 + 一个 demo dashboard）

**M2 编辑器 MVP（W3-W6）**

* W3：画布框架（React + Konva + Zustand 装好；4 个区域 Group 渲染）
* W4：蒙版加载与裁剪；素材拖拽 API
* W5：参数面板（密度 / 旋转 / 间距）+ 实时预览 + Konva cache 优化
* W6：草稿保存 + 历史版本 + 设计稿列表页

**M3 AI 与延伸（W7-W9）**

* W7：AI 生图代理服务（接通义万相）+ 内容审核接入 + Celery worker
* W8：自动填充策略（平铺 / 拉伸 / 智能分块）+ 前端进度条（SSE）
* W9：款式延伸（ControlNet 接入 + 1/2/4 选项 UI + gallery 展示）

**M4 订单与小程序（W10-W12）**

* W10：订单提交流程 + 爱花型订单 API 对接 + 支付页跳转
* W11：UniApp 小程序（共享业务代码 + 移动端布局适配 + 微信小程序构建上传）
* W12：灰度发布（10% 用户 → 50% → 100%）+ 监控 + 上线前回归测试

### 8.3 团队配置

| 角色 | 投入 |
|------|------|
| 后端工程师 | 1.5 人 × 12 周 |
| 前端工程师（Web） | 1 人 × 12 周 |
| 小程序工程师 | 0.5 人 × 后 6 周 |
| AI/算法工程师（兼） | 0.5 人 × 中间 6 周（M3 集中投入）|
| UI 设计师 | 0.5 人 × 全程 |
| 测试 | 0.5 人 × 后 6 周 |
| 项目经理 | 0.3 人 × 全程 |

总人月约 **6.5 人月**。

### 8.4 关键风险与缓冲

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| AI 生图 API 在国内断供（合规问题） | 中 | 高 | 备用自托管 SD；预先签订两家供应商合同 |
| 爱花型订单字段后期变更 | 中 | 中 | 订单参数表用配置驱动，避免硬编码字段 |
| 小程序审核被拒（涉及 AI 生图） | 中 | 中 | 加强内容审核 + 上线前与微信平台沟通 |
| 用户系统 SSO 协议不稳 | 低 | 高 | 与现有服务商前置 1-2 周做联调 demo |
| Konva 在低端设备性能不足 | 低 | 中 | 增加 fallback 模式（关闭实时预览改手动刷新）|

每个 milestone 末尾预留 2-3 天 buffer 应付任何风险。整个 12 周计划中预埋约 1 周机动时间。

---

## 九、验收标准

### 9.1 功能验收

* [ ] 用户能在 Web 端用一句话生成图案 → 自动填充到袜版四区
* [ ] 用户能拖拽素材库的图片到任意袜版区域
* [ ] 用户能调整密度 / 旋转 / 间距 三个参数，画布实时反馈
* [ ] 用户能点款式延伸生成 1/2/4 张同款变体
* [ ] 用户能选择变体替换当前画布
* [ ] 用户能填写订单参数并一键提交到爱花型
* [ ] 用户能在小程序端完成同样的设计 → 下单流程（功能完备但布局适配移动端）
* [ ] 新用户注册后自动同步建立爱花型账号
* [ ] 老用户能用爱花型账号直接登录

### 9.2 性能验收

| 指标 | 目标 |
|------|------|
| 首屏加载时间 | ≤ 2.5s |
| 画布拖拽帧率 | ≥ 50 fps |
| 单张 AI 生图端到端耗时 | ≤ 15s（P95）|
| 款式延伸 4 张总耗时 | ≤ 30s（P95）|
| 订单提交成功率 | ≥ 99.5% |
| AI 生图任务排队最长时间 | ≤ 60s |

### 9.3 质量验收

* 单元测试覆盖率 ≥ 70%
* 关键路径（注册 / 登录 / 设计 / 下单）E2E 测试自动化 100%
* 通过阿里云内容安全审核（备案合规）
* 主流浏览器（Chrome / Edge / Safari / 微信内置）兼容性测试通过
* 无障碍 WCAG AA 评分通过

### 9.4 交付物清单

* [ ] 完整源代码（Git 仓库）
* [ ] 部署文档（K8s YAML + 环境变量清单）
* [ ] 接口文档（OpenAPI 3.0 + Swagger UI）
* [ ] 数据库迁移脚本与初始数据
* [ ] 运维监控 dashboard 配置
* [ ] 用户使用手册（图文版 + 视频版）
* [ ] 一年内 bug 维护承诺（每月 ≤ 5 个非紧急 bug 在 5 工作日内修复）

---

## 十、总结

爱花型袜版设计系统是一个把 AI 能力工程化嫁接到现有袜业平台的产品。技术上不复杂——Konva + FastAPI + 第三方 AI API + 爱花型现有订单/用户系统，但**胜在把链路串得短**：从灵感到下单 5 分钟。

执行的关键点：

1. **先把"待确认 7 项"敲定再开发**。任何一项变更（特别是订单字段和小程序选型）会影响 1-2 周工作量。
2. **AI 生图链路要预留两套供应商**。国内合规环境下任一家断供都不影响业务，工程上要有切换能力。
3. **画布性能是产品体验的命脉**。Konva 的 `cache()`、防抖、虚拟化滚动这些细节决定了产品"流畅 vs 卡顿"。
4. **款式延伸是差异化卖点**。通用画图工具没有"一键出 4 个变体"的能力，要花精力把 ControlNet + prompt 模板调好。
5. **订单链路必须 99.5%+ 可靠性**。袜业用户对"下单不成功"的容忍度极低，宁可前端报错也不能静默失败。

12 周 6.5 人月的投入，能交付一个可商用的 v1.0。后续 v1.1 可加企业版（批量下单 / 企业 logo / 自定义袜型），v1.2 可加 AI 设计师助手（基于历史下单推荐风格）。
