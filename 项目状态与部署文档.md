# 爱花型 · AI 袜版设计系统 — 项目状态与部署文档

> 最后更新：2026-05-31（PC 网页微信扫码登录；账号合并；多端数据同步说明 + 小程序上传 Checklist）
> 仓库：https://github.com/cryangle0/AISock.git （分支 `main`）
> 线上域名：https://onnsa.cn

---

## 一、项目概览

爱花型是一套「AI 袜版定制」全栈系统，由四个子工程组成，统一放在一个仓库（monorepo 思路，各子目录独立构建）：

| 子工程 | 目录 | 技术栈 | 线上地址 |
|--------|------|--------|----------|
| 后端服务 | `server/` | Hono + TypeScript + MySQL + Redis（tsx 直跑，无构建） | https://onnsa.cn/aisock-api |
| Web 前端 | `web/` | Vue3 + Vite + Pinia + Vue Router | https://onnsa.cn/aisock/ |
| 运营后台 | `admin/` | Vue3 + Arco Design + Pinia + Vue Router | https://onnsa.cn/aisock-admin/ |
| 微信小程序 | `miniprogram/` | uni-app + Vue3 + Pinia（pnpm workspace 多包） | 微信小程序（需开发者工具上传） |
| 设计原型 | `prototype/` | React + Vite（仅作视觉/交互参考，不部署） | — |

核心业务：用户选袜型 → 选/上传/AI 生成花型 → 配色与色卡映射 → 袜面布局编辑 → 预览 → 保存设计 → 下单 → 支付 → 订单/物流跟踪。运营后台管理袜型、花型、订单、用户、Banner、推荐资讯、小程序首页配置、AI 任务监控。

---

## 二、业务功能完成度

### 2.1 后端（server/）— 完整
- 鉴权：手机号短信验证码登录、微信 code2session 登录、注销、邀请关系
- 目录：首页聚合、袜型、花型（公共库 + 个人库）、分类
- 设计：保存/更新/删除/详情，封面 dataURL 自动转存 OSS，regions 完整快照
- 订单：创建、列表、详情、统计、备注/地址更新
- 支付：微信支付 V3（无第三方依赖，自实现签名/验签/AEAD 回调解密），缺商户证书时自动降级 mock
- AI：文/图生图（KIE nano-banana 协议）、每日配额、款式衍生、亲子袜、邀请奖励
- 上传：本地磁盘 / 阿里云 OSS（无依赖 V1 签名）
- 推荐流/资讯、物流轨迹、运营配置（app_config）
- 运营后台全套接口：袜型/花型/订单/用户/Banner/文章/物流/AI 任务/小程序配置

### 2.2 Web 前端（web/）— 完整且已端到端验证
- 登录（短信 + **PC 微信扫码**）、首页、推荐、我的、购物车/订单
- 设计器：真实 Canvas 渲染引擎（`web/src/engine/`，像素蒙版分区/印花裁剪/色卡映射/平铺/旋转/导出）
- AI 文生图 + OSS 转存、四区配色、色卡映射、调节、查看蒙版、点击袜版定位颜色
- 款式衍生/亲子袜（真实离屏渲染缩略图）、分享
- 下单 → 支付 → 落库闭环（演示用 mock-paid，订单真实落库）
- **站点品牌后台可配**：logo / 站点标题 / 品牌名 / 登录页标题副标题 / 版权 / favicon 由 `siteConfig` store 启动拉取并响应式驱动顶栏、登录页、document 标题与图标；首页资讯中心接后端 feed/news（带兜底）

### 2.3 运营后台（admin/）— 完整
- 仪表盘、袜型管理、花型素材（含编辑 + 新增分类）、订单管理（含订单详情抽屉）、用户管理、Banner 管理
- **小程序配置**（首页主题/功能区/案例，可视化增删改排序 + 启停，保存约 1 分钟生效）
- **AI 模型配置**：按平台（默认 / 小程序 / Web）配置文/图生图模型、提示词模板、出图比例；平台留空沿用默认
- **站点配置**：Web/登录页的 logo、站点标题、品牌名、副标题、版权可视化编辑 + 实时预览
- 推荐/资讯、物流管理、AI 监控（任务统计 + 列表，只读观测）

### 2.4 微信小程序（miniprogram/）— 功能完整，视觉对齐原型
- 页面：首页/推荐/AI设计(编辑器)/购物车/我的/登录/我的设计/订单/订单详情/素材库
- 首页：棕色品牌带 + 敦煌梦 banner + 主题随心订 + 3D 横滑案例 carousel + 功能区（接后台配置带兜底）
- 设计器：canvas 2d 矢量渲染真实袜形、花型/配色/调节/色卡、款式衍生、亲子袜、保存（regions 可再编辑）
- 下单 → 支付（`composables/usePayment`：有证书走真实 `uni.requestPayment`，否则 mock）→ 落库
- 素材上传走 `/upload` 转 OSS、AI 配额、分享
- 目录数据（袜型/花型）后端优先 + 本地兜底（`composables/useCatalog`，运营后台可控）

#### 2.4.1 安全加固与功能闭环（2026-05-31 全量修复）
- **支付安全**：订单单价/总价改由服务端 `pricing.service` 按「材质+工艺+数量」权威计算，
  前端传值不可篡改；移除「直接标记已付」接口；`/pay/mock-paid` 生产环境且具备真实支付能力时禁用；
  微信登录生产环境禁止直接传 openid（必须 `uni.login` 的 code 换取）。
- **款式衍生/亲子袜**：改为服务端 `variant.service` 下发结构化「配方」（配色+排布，运营可控、多端一致），
  客户端套用到「用户当前印花」（含 AI 生成图）渲染预览；亲子套装保存补全完整设计数据（regions）可再编辑。
- **目录数据**：袜型/花型接入后端（`/socks`、`/patterns`），本地常量降级为兜底；编辑器印花网格支持
  「内置矢量花型 + 后端图片印花」统一展示与应用。
- **新增接口**：`GET /orders/pricing`（价目表）、`POST /orders/quote`（价格试算）。
- **组件化**：新增 `packages/app/src/composables/`（usePayment / useVariants / variantRender / useCatalog），
  逻辑从页面下沉、单一职责、可复用。
- **收尾**：登录页协议可查看、移除假 openid 兜底；我的「设置」可用；订单详情「联系客服」可拨打；
  分享链接域名统一为 `SHARE_BASE_URL`（onnsa.cn）。

> 已知前置条件：① 微信支付商户私钥 `apiclient_key.pem` 上传后真实支付自动生效（当前 mock，订单真实落库）；
> ② 微信一键拿手机号（`getPhoneNumber`）后端能力已就绪，待小程序**完成认证**后将登录按钮切回 `open-type=getPhoneNumber` 即可启用（当前手机号走阿里云短信验证码）。

---

## 三、服务器部署情况（阿里云 ECS）

> SSH 别名：`ssh onnsa`（公网 IP 47.96.2.138，CentOS / Alibaba Cloud Linux）
> 复用宿主已有的 nginx、MySQL、Redis、pm2，**与服务器上其它项目（onnsa 主站等）隔离，互不影响**。

### 3.1 运行环境
- Node.js：v22.15.1
- 进程管理：pm2，进程名 `aisock-server`（fork 模式，online）
- 端口：后端 `127.0.0.1:8199`，MySQL `3306`，Redis `6379`

### 3.2 目录结构（服务器）
| 用途 | 路径 |
|------|------|
| 代码仓库 | `/root/work/aisock/`（git pull 同步） |
| 后端运行目录 | `/root/work/aisock/server`（pm2 cwd） |
| Web 静态文件 | `/var/www/aisock/` |
| 后台静态文件 | `/var/www/aisock-admin/` |
| 上传文件（本地兜底） | `/root/work/aisock/server/uploads/` |
| 微信支付证书（待放置） | `/root/work/aisock/server/certs/apiclient_key.pem` |

### 3.3 nginx 路由（写在 `/etc/nginx/conf.d/door.conf` 内 onnsa.cn 443 server 块，独立 location 段）
- `/aisock-api/` → 反代 `http://127.0.0.1:8199/`（后端 API，含 600s 超时、20M body）
- `/aisock/` → `/var/www/aisock/`（Web SPA，try_files 回退 index.html）
- `/aisock-admin/` → `/var/www/aisock-admin/`（后台 SPA）

### 3.4 数据库（MySQL 库名 `aisock`）
已建表：`admin_account, ai_task, app_config, article, banner, design, invitation, order, order_shipment, payment, pattern, pattern_category, sock_model, upload, user`
迁移脚本：`server/migrations/001_init.sql`、`002_business.sql`、`003_app_config.sql`（均已应用）
Redis：使用 db 5（与其它项目隔离）

### 3.5 线上健康检查（当前全部 200）
- `https://onnsa.cn/aisock-api/api/v1/app/home` → 200
- `https://onnsa.cn/aisock/` → 200
- `https://onnsa.cn/aisock-admin/` → 200

---

## 四、关键配置与凭证

### 4.1 后端环境变量（服务器 `server/.env`，本地参考 `.deploy-server.env`，含密钥不入库）
| 项 | 值 / 说明 |
|----|-----------|
| PORT | 8199 |
| DB | 127.0.0.1:3306 / 库 aisock |
| Redis | 127.0.0.1:6379 / db 5 |
| JWT_SECRET | 已配生产随机串，有效期 7d |
| 短信 | 阿里云，签名「杭州昂撒科技」，测试码 `SMS_MASTER_CODE=888888` |
| OSS | bucket `onnsa`，CDN 域名 `cdn.onnsa.cn` |
| AI 图 | KIE `https://api.kie.ai`（key 已配，余额需客户维护） |
| AI 文 | DeepSeek |
| 微信小程序 | `WX_APPID=wx3a64370afc288cc4`，`WX_SECRET=e46136fd...`（32位已确认，真实 code2session/openid 登录已线上验证） |
| 微信支付 | 商户号 1717685486、序列号、APIv3 key 已配；**商户私钥 apiclient_key.pem 待客户上传** |

### 4.2 前端环境变量
- `web/.env.production`：`VITE_API_BASE_URL=https://onnsa.cn/aisock-api`
- `admin/.env.production`：`VITE_API_BASE_URL=https://onnsa.cn/aisock-api`
- 构建时设 `VITE_BASE`：web 用 `/aisock/`，admin 用 `/aisock-admin/`
- 小程序 API 基址：`miniprogram/packages/common/src/constants/index.ts` 中 `API_BASE_URL=https://onnsa.cn/aisock-api`

### 4.3 默认账号
- 运营后台：`admin / admin123`
- 小程序/Web 测试登录：任意手机号 + 验证码 `888888`（master code）

---

## 五、构建与部署流程

### 5.1 后端（无需构建，tsx 直跑）
```bash
ssh onnsa
cd /root/work/aisock && git pull origin main
cd server && npm install        # 仅依赖变化时
pm2 restart aisock-server
```
> 注：若服务器对 GitHub 拉取超时，可先 `git fetch origin && git reset --hard origin/main`（服务器无本地改动时安全）。

### 5.2 Web 前端
```bash
# 本地
cd web
# Windows PowerShell: $env:VITE_BASE="/aisock/"; npm run build
npm run build                   # 需带 VITE_BASE=/aisock/
# 打包 dist 上传，解压到 /var/www/aisock/
```

### 5.3 运营后台
```bash
cd admin
# VITE_BASE=/aisock-admin/ npm run build
# 打包 dist 上传，解压到 /var/www/aisock-admin/
```

### 5.4 微信小程序
```bash
cd miniprogram/packages/app
npm run build:mp-weixin
# 用微信开发者工具导入 dist/build/mp-weixin 目录 → 上传
```

---

## 六、待客户提供 / 后续事项

1. **微信支付商户私钥** `apiclient_key.pem`：从微信商户平台下载，放到服务器 `/root/work/aisock/server/certs/`，重启后端后真实支付自动生效（当前为 mock 演示态，订单仍真实落库）。
2. **微信支付与小程序关联**：商户平台需把小程序 AppID `wx3a64370afc288cc4` 与商户号 `1717685486` 完成绑定。
3. **微信小程序服务器域名配置**（公众平台 → 开发设置 → 服务器域名）：
   - request 合法域名：`https://onnsa.cn`
   - uploadFile 合法域名：`https://onnsa.cn`
   - downloadFile 合法域名：`https://cdn.onnsa.cn`
4. **KIE AI 余额**：客户在 kie.ai 维护账户余额，余额不足时 AI 出图自动回退占位图（不阻断业务）。
5. **小程序上传发布**：用微信开发者工具导入 `dist/build/mp-weixin` 后上传、提审。

---

## 七、品牌资产

- Logo：仓库根 `logo.png`（已部署到 web/admin public，用于浏览器 tab favicon、顶栏/侧栏品牌标、登录页）
- 主题：敦煌风（暖米底 + 棕色 + 矿物质感色），主色 `#8c5a3c`，艺术字 ZCOOL KuaiLe
- Web/后台登录页：原型双栏布局（左品牌视觉 + 右登录卡）

---

## 八、版本基线

- 当前 `main` 最新提交：`f799cc5`（PC 网页微信扫码登录：小程序码 + 轮询，账号天然统一）
  - `a955df5`：短信登录自动合并微信 openid 临时账号
  - `3537707`：Web 站点品牌后台可配 + 后台缺口补齐
  - `aa70074`：后台可配置小程序/Web 生成模型
  - `5fb7cca`：设计/支付/目录全链路闭环 + 组件化重构
  - 更早：`a9dec94` 支付安全加固、`169a1a4` 微信登录真实化、`b407cb2` 小程序 UI/滚动
- 服务器后端运行代码：与最新一致（含 ai/pricing/variant/aiConfig/siteConfig 等 services 与对应 routes，均 scp 同步 + `pm2 restart`，服务器 `tsc --noEmit` 通过、线上接口实测正确）
- **Web/后台静态文件已部署**：`/var/www/aisock`（web，VITE_BASE=/aisock/）、`/var/www/aisock-admin`（admin，VITE_BASE=/aisock-admin/），线上 admin=200 / web=200
- 服务器代码目录 git 指针：仍指向 `0c4884e`（服务器拉取 GitHub 偶发超时；运行内容已最新，不影响线上）

> 提示：后端运行只依赖 server/ 目录。后端改动已通过 scp 同步到服务器并重启验证。
> 待服务器到 GitHub 网络恢复后，执行 `git fetch && git reset --hard origin/main` 让代码目录 git 指针与最新对齐（不影响线上运行）。
> 小程序前端改动需用微信开发者工具重新构建 `dist/build/mp-weixin` 后上传提审才会在线上生效。

### 后台可配置能力总览
- **小程序首页配置**（app_config: home_themes/home_zones/home_cases）
- **AI 模型配置**（app_config: ai_generation）：按平台配文/图生图模型、提示词模板、出图比例
- **站点品牌配置**（app_config: site_config）：Web logo/标题/品牌名/副标题/版权/favicon
- 袜型 / 花型（含编辑、新增分类）/ Banner / 推荐资讯 / 物流 / 用户配额 等常规 CRUD

### 线上验证记录（2026-05-31）
- 计价 `POST /orders/quote`（nylon+3d×80）→ 单价 38、总价 3040；防篡改 unitPrice 被忽略 ✓
- AI 配置 `GET/PUT /admin/ai-config`：保存后平台覆盖生效、留空回退默认 ✓
- 站点配置 `GET/PUT /admin/site-config` + 公开 `GET /app/site-config`：写入后公开端即时反映 ✓
- 静态站点：`https://onnsa.cn/aisock-admin/` = 200、`https://onnsa.cn/aisock/` = 200

### 微信登录说明（方案 A）
- 微信登录仅取 openid 身份，**手机号统一走阿里云短信验证码**（与微信认证无关）。
- 后端已具备 `getPhoneNumber` 一键授权换手机号能力（access_token 缓存、账号合并），**待小程序完成认证后**将登录按钮改回 `open-type="getPhoneNumber"` 即可启用，无需改后端。
- `WX_SECRET` 已确认为正确 32 位值 `e46136fd747cb2e09706f1d1c8e3dfc6`。

### 安全待办（上线前必处理）
- `SMS_MASTER_CODE=888888` 万能验证码后门：当前应客户要求保留，**正式上线前务必从服务器 `.env` 删除**，否则任意手机号可用 888888 登录。

---

## 九、多端数据同步说明

- **小程序与 Web 连同一后端、同一数据库**（基址均 `https://onnsa.cn/aisock-api`），数据按 `user.id` 归属。
- **数据互通的前提是「同一用户身份」**：
  - 两端都用**同一手机号短信登录** → 同一 `user.id`，设计/订单/AI 配额完全互通。✅
  - Web 短信账号 与 小程序**微信(openid)登录** → 默认是两个 `user.id`，数据各自独立。
- **账号合并保险（已实现）**：
  - 微信登录拿到真实手机号时（小程序认证后启用 `getPhoneNumber`）→ 自动按手机号合并到已有账号。
  - 当前方案 A 下，用户先微信登录、再用同手机号短信登录时 → 后端自动把「无手机号的 openid 临时账号」的设计/订单/花型/AI 任务迁移到手机号账号并补绑 openid，最后删除临时账号（事务保证一致性）。
- **共享内容**：袜型/花型/Banner/资讯/AI 模型配置/站点配置等来自同一后端，两端天然一致。
- **Web 端微信登录**：支持 **PC 扫码登录**（方案：小程序码 + 轮询）。Web 生成小程序码 → 微信扫码打开小程序确认页 → 已登录用户确认 → Web 轮询拿 token。复用现有小程序、无需开放平台网站应用资质；PC 直接复用小程序 `user.id`，**多端账号天然统一**。前置：`getUnlimited` 生成小程序码要求小程序**已发布**；未发布/未配密钥时退回手机号登录。

## 十、小程序上传前 Checklist

代码侧（已核对 OK）：
- manifest `mp-weixin.appid = wx3a64370afc288cc4`；无本地调试地址残留（基址为 HTTPS 线上）；最新代码已 `npm run build:mp-weixin`。

微信公众平台后台需配置：
- **服务器域名**（开发设置 → 服务器域名）：
  - request / uploadFile 合法域名：`https://onnsa.cn`
  - downloadFile 合法域名：`https://onnsa.cn`、`https://cdn.onnsa.cn`
- 业务域名：如需小程序内打开分享 H5（`onnsa.cn/aisock`）按需配 web-view 业务域名。

上传步骤：
1. 开发者工具导入 `miniprogram/packages/app/dist/build/mp-weixin`，填 AppID。
2. 编译预览，确认登录/设计/下单/AI 生图正常。
3. 上传 → 公众平台「版本管理」提交审核 → 通过后发布。

上线前另需：微信支付私钥 `apiclient_key.pem` 上传 + 商户号绑定；小程序认证后启用 `getPhoneNumber`；删除 `SMS_MASTER_CODE`；维护 KIE AI 余额。
