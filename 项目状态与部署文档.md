# 爱花型 · AI 袜版设计系统 — 项目状态与部署文档

> 最后更新：2026-06-13（第四轮深度审计：支付死路/价格口径/AI 对话边界/后台配置安全，三端修复）
> 仓库：https://github.com/cryangle0/AISock.git （分支 `main`）
> 线上域名：https://onnsa.cn

---

## 一、项目概览

爱花型是一套「AI 袜版定制」全栈系统，由四个子工程组成，统一放在一个仓库（monorepo 思路，各子目录独立构建）：

| 子工程 | 目录 | 技术栈 | 线上地址 |
|--------|------|--------|----------|
| 后端服务 | `server/` | Hono + TypeScript + MySQL + Redis（tsx 直跑，无构建） | https://onnsa.cn/aisock-api |
| Web 前端 | `web/` | Vue3 + Vite + Pinia + Vue Router | https://onnsa.cn/aisock/ |
| 运营后台 | `admin/` | Vue3 + Arco Design + Pinia + Vue Router | https://onnsa.cn/aisock-admin/ |
| 微信小程序 | `miniprogram/` | uni-app + Vue3 + Pinia（pnpm workspace 多包） | 微信小程序（需开发者工具上传） |
| 设计原型 | `prototype/` | React + Vite（仅作视觉/交互参考，不部署） | — |

核心业务：用户选袜型 → 选/上传/AI 生成花型 → 配色与色卡映射 → 袜面布局编辑 → 预览 → 保存设计 → 下单 → 支付 → 订单/物流跟踪。运营后台管理袜型、花型、订单、用户、Banner、推荐资讯、小程序首页配置、AI 任务监控。

---

## 二、业务功能完成度

### 2.1 后端（server/）— 完整
- 鉴权：手机号短信验证码登录、微信 code2session 登录、注销、邀请关系
- 目录：首页聚合、袜型、花型（公共库 + 个人库）、分类
- 设计：保存/更新/删除/详情，封面 dataURL 自动转存 OSS，regions 完整快照
- 订单：创建、列表、详情、统计、备注/地址更新
- 支付：微信支付 V3（无第三方依赖，自实现签名/验签/AEAD 回调解密），缺商户证书时自动降级 mock
- AI：文/图生图（KIE nano-banana 协议）、每日配额、款式衍生、亲子袜、邀请奖励
- 上传：本地磁盘 / 阿里云 OSS（无依赖 V1 签名）
- 推荐流/资讯、物流轨迹、运营配置（app_config）
- 运营后台全套接口：袜型/花型/订单/用户/Banner/文章/物流/AI 任务/小程序配置

### 2.2 Web 前端（web/）— 完整且已端到端验证
- 登录（短信 + **PC 微信扫码**）、首页、推荐、我的、购物车/订单
- 设计器：真实 Canvas 渲染引擎（`web/src/engine/`，像素蒙版分区/印花裁剪/色卡映射/平铺/旋转/导出）
- AI 文生图 + OSS 转存、四区配色、色卡映射、调节、查看蒙版、点击袜版定位颜色
- 款式衍生/亲子袜（真实离屏渲染缩略图）、分享
- 下单 → 支付 → 落库闭环（演示用 mock-paid，订单真实落库）
- **站点品牌后台可配**：logo / 站点标题 / 品牌名 / 登录页标题副标题 / 版权 / favicon 由 `siteConfig` store 启动拉取并响应式驱动顶栏、登录页、document 标题与图标；首页资讯中心接后端 feed/news（带兜底）

### 2.3 运营后台（admin/）— 完整
- 仪表盘、袜型管理、花型素材（含编辑 + 新增分类）、订单管理（含订单详情抽屉）、用户管理、Banner 管理
- **小程序配置**（首页主题/功能区/案例，可视化增删改排序 + 启停，保存约 1 分钟生效）
- **AI 模型配置**：按平台（默认 / 小程序 / Web）配置文/图生图模型、提示词模板、出图比例；平台留空沿用默认
- **站点配置**：Web/登录页的 logo、站点标题、品牌名、副标题、版权可视化编辑 + 实时预览
- 推荐/资讯、物流管理、AI 监控（任务统计 + 列表，只读观测）

### 2.4 微信小程序（miniprogram/）— 功能完整，视觉对齐原型
- 页面：首页/推荐/AI设计(编辑器)/购物车/我的/登录/我的设计/订单/订单详情/素材库
- 首页：棕色品牌带 + 敦煌梦 banner + 主题随心订 + 3D 横滑案例 carousel + 功能区（接后台配置带兜底）
- 设计器：canvas 2d 矢量渲染真实袜形、花型/配色/调节/色卡、款式衍生、亲子袜、保存（regions 可再编辑）
- 下单 → 支付（`composables/usePayment`：有证书走真实 `uni.requestPayment`，否则 mock）→ 落库
- 素材上传走 `/upload` 转 OSS、AI 配额、分享
- 目录数据（袜型/花型）后端优先 + 本地兜底（`composables/useCatalog`，运营后台可控）

#### 2.4.1 安全加固与功能闭环（2026-05-31 全量修复）
- **支付安全**：订单单价/总价改由服务端 `pricing.service` 按「材质+工艺+数量」权威计算，
  前端传值不可篡改；移除「直接标记已付」接口；`/pay/mock-paid` 生产环境且具备真实支付能力时禁用；
  微信登录生产环境禁止直接传 openid（必须 `uni.login` 的 code 换取）。
- **款式衍生/亲子袜**：改为服务端 `variant.service` 下发结构化「配方」（配色+排布，运营可控、多端一致），
  客户端套用到「用户当前印花」（含 AI 生成图）渲染预览；亲子套装保存补全完整设计数据（regions）可再编辑。
- **目录数据**：袜型/花型接入后端（`/socks`、`/patterns`），本地常量降级为兜底；编辑器印花网格支持
  「内置矢量花型 + 后端图片印花」统一展示与应用。
- **新增接口**：`GET /orders/pricing`（价目表）、`POST /orders/quote`（价格试算）。
- **组件化**：新增 `packages/app/src/composables/`（usePayment / useVariants / variantRender / useCatalog），
  逻辑从页面下沉、单一职责、可复用。
- **收尾**：登录页协议可查看、移除假 openid 兜底；我的「设置」可用；订单详情「联系客服」可拨打；
  分享链接域名统一为 `SHARE_BASE_URL`（onnsa.cn）。

> 已知前置条件：① 微信支付商户私钥 `apiclient_key.pem` 上传后真实支付自动生效（当前 mock，订单真实落库）；
> ② 微信一键拿手机号（`getPhoneNumber`）后端能力已就绪，待小程序**完成认证**后将登录按钮切回 `open-type=getPhoneNumber` 即可启用（当前手机号走阿里云短信验证码）。

---

## 三、服务器部署情况（阿里云 ECS）

> SSH 别名：`ssh onnsa`（公网 IP 47.96.2.138，CentOS / Alibaba Cloud Linux）
> 复用宿主已有的 nginx、MySQL、Redis、pm2，**与服务器上其它项目（onnsa 主站等）隔离，互不影响**。

### 3.1 运行环境
- Node.js：v22.15.1
- 进程管理：pm2，进程名 `aisock-server`（fork 模式，online）
- 端口：后端 `127.0.0.1:8199`，MySQL `3306`，Redis `6379`

### 3.2 目录结构（服务器）
| 用途 | 路径 |
|------|------|
| 代码仓库 | `/root/work/aisock/`（git pull 同步） |
| 后端运行目录 | `/root/work/aisock/server`（pm2 cwd） |
| Web 静态文件 | `/var/www/aisock/` |
| 后台静态文件 | `/var/www/aisock-admin/` |
| 上传文件（本地兜底） | `/root/work/aisock/server/uploads/` |
| 微信支付证书（待放置） | `/root/work/aisock/server/certs/apiclient_key.pem` |

### 3.3 nginx 路由（写在 `/etc/nginx/conf.d/door.conf` 内 onnsa.cn 443 server 块，独立 location 段）
- `/aisock-api/` → 反代 `http://127.0.0.1:8199/`（后端 API，含 600s 超时、20M body）
- `/aisock/` → `/var/www/aisock/`（Web SPA，try_files 回退 index.html）
- `/aisock-admin/` → `/var/www/aisock-admin/`（后台 SPA）

### 3.4 数据库（MySQL 库名 `aisock`）
已建表：`admin_account, ai_task, app_config, article, banner, design, invitation, order, order_shipment, payment, pattern, pattern_category, sock_model, upload, user`
迁移脚本：`server/migrations/001_init.sql`、`002_business.sql`、`003_app_config.sql`（均已应用）
Redis：使用 db 5（与其它项目隔离）

### 3.5 线上健康检查（当前全部 200）
- `https://onnsa.cn/aisock-api/api/v1/app/home` → 200
- `https://onnsa.cn/aisock/` → 200
- `https://onnsa.cn/aisock-admin/` → 200

---

## 四、关键配置与凭证

### 4.1 后端环境变量（服务器 `server/.env`，本地参考 `.deploy-server.env`，含密钥不入库）
| 项 | 值 / 说明 |
|----|-----------|
| PORT | 8199 |
| DB | 127.0.0.1:3306 / 库 aisock |
| Redis | 127.0.0.1:6379 / db 5 |
| JWT_SECRET | 已配生产随机串，有效期 7d |
| 短信 | 阿里云，签名「杭州昂撒科技」，测试码 `SMS_MASTER_CODE=888888` |
| OSS | bucket `onnsa`，CDN 域名 `cdn.onnsa.cn` |
| AI 图 | KIE `https://api.kie.ai`（key 已配，余额需客户维护） |
| AI 文 | DeepSeek |
| 微信小程序 | `WX_APPID=wx3a64370afc288cc4`，`WX_SECRET=e46136fd...`（32位已确认，真实 code2session/openid 登录已线上验证） |
| 微信支付 | 商户号 1717685486、序列号、APIv3 key 已配；**商户私钥 apiclient_key.pem 待客户上传** |

### 4.2 前端环境变量
- `web/.env.production`：`VITE_API_BASE_URL=https://onnsa.cn/aisock-api`
- `admin/.env.production`：`VITE_API_BASE_URL=https://onnsa.cn/aisock-api`
- 构建时设 `VITE_BASE`：web 用 `/aisock/`，admin 用 `/aisock-admin/`
- 小程序 API 基址：`miniprogram/packages/common/src/constants/index.ts` 中 `API_BASE_URL=https://onnsa.cn/aisock-api`

### 4.3 默认账号
- 运营后台：`admin / admin123`
- 小程序/Web 测试登录：任意手机号 + 验证码 `888888`（master code）

---

## 五、构建与部署流程

### 5.1 后端（无需构建，tsx 直跑）
```bash
ssh onnsa
cd /root/work/aisock && git pull origin main
cd server && npm install        # 仅依赖变化时
pm2 restart aisock-server
```
> 注：若服务器对 GitHub 拉取超时，可先 `git fetch origin && git reset --hard origin/main`（服务器无本地改动时安全）。

### 5.2 Web 前端
```bash
# 本地
cd web
# Windows PowerShell: $env:VITE_BASE="/aisock/"; npm run build
npm run build                   # 需带 VITE_BASE=/aisock/
# 打包 dist 上传，解压到 /var/www/aisock/
```

### 5.3 运营后台
```bash
cd admin
# VITE_BASE=/aisock-admin/ npm run build
# 打包 dist 上传，解压到 /var/www/aisock-admin/
```

### 5.4 微信小程序
```bash
cd miniprogram/packages/app
npm run build:mp-weixin
# 用微信开发者工具导入 dist/build/mp-weixin 目录 → 上传
```

---

## 六、待客户提供 / 后续事项

1. **微信支付商户私钥** `apiclient_key.pem`：从微信商户平台下载，放到服务器 `/root/work/aisock/server/certs/`，重启后端后真实支付自动生效（当前为 mock 演示态，订单仍真实落库）。
2. **微信支付与小程序关联**：商户平台需把小程序 AppID `wx3a64370afc288cc4` 与商户号 `1717685486` 完成绑定。
3. **微信小程序服务器域名配置**（公众平台 → 开发设置 → 服务器域名）：
   - request 合法域名：`https://onnsa.cn`
   - uploadFile 合法域名：`https://onnsa.cn`
   - downloadFile 合法域名：`https://cdn.onnsa.cn`
4. **KIE AI 余额**：客户在 kie.ai 维护账户余额，余额不足时 AI 出图自动回退占位图（不阻断业务）。
5. **小程序上传发布**：用微信开发者工具导入 `dist/build/mp-weixin` 后上传、提审。

---

## 七、品牌资产

- Logo：仓库根 `logo.png`（已部署到 web/admin public，用于浏览器 tab favicon、顶栏/侧栏品牌标、登录页）
- 主题：敦煌风（暖米底 + 棕色 + 矿物质感色），主色 `#8c5a3c`，艺术字 ZCOOL KuaiLe
- Web/后台登录页：原型双栏布局（左品牌视觉 + 右登录卡）

---

## 八、版本基线

- 当前 `main` 最新提交：`f74b092`（对照合同补齐：设计导出 PNG/JPG + 意图分析 + AI 改色 + 订单编辑/附件 + 密码登录，三端 + 后台）
  - `d239a9f`：修复 admin 小程序配置排序 bug
  - `c6dca66`：补齐未完成功能（真实分享/邀请 + Web 订单详情/物流 + 首页死链清理）
  - `beae5fa`：收尾安全修复（上传/事务/会话/并发）
  - `eca18ae`：支付/鉴权/配额 安全加固
  - `4c00064`：袜型差异化渲染 + 小程序色卡真实映射
  - 更早：`106a580` 业务逻辑修复、`f799cc5` 扫码登录、`a955df5` 账号合并、`3537707` 站点配置、`aa70074` AI 模型配置
- 服务器后端运行代码：与最新一致（含 ai/pricing/variant/aiConfig/siteConfig 等 services 与对应 routes，均 scp 同步 + `pm2 restart`，服务器 `tsc --noEmit` 通过、线上接口实测正确）
- **Web/后台静态文件已部署**：`/var/www/aisock`（web，VITE_BASE=/aisock/）、`/var/www/aisock-admin`（admin，VITE_BASE=/aisock-admin/），线上 admin=200 / web=200
- 服务器代码目录 git 指针：仍指向 `0c4884e`（服务器拉取 GitHub 偶发超时；运行内容已最新，不影响线上）

> 提示：后端运行只依赖 server/ 目录。后端改动已通过 scp 同步到服务器并重启验证。
> 待服务器到 GitHub 网络恢复后，执行 `git fetch && git reset --hard origin/main` 让代码目录 git 指针与最新对齐（不影响线上运行）。
> 小程序前端改动需用微信开发者工具重新构建 `dist/build/mp-weixin` 后上传提审才会在线上生效。

### 后台可配置能力总览
- **小程序首页配置**（app_config: home_themes/home_zones/home_cases）
- **AI 模型配置**（app_config: ai_generation）：按平台配文/图生图模型、提示词模板、出图比例
- **站点品牌配置**（app_config: site_config）：Web logo/标题/品牌名/副标题/版权/favicon
- 袜型 / 花型（含编辑、新增分类）/ Banner / 推荐资讯 / 物流 / 用户配额 等常规 CRUD

### 线上验证记录（2026-05-31）
- 计价 `POST /orders/quote`（nylon+3d×80）→ 单价 38、总价 3040；防篡改 unitPrice 被忽略 ✓
- AI 配置 `GET/PUT /admin/ai-config`：保存后平台覆盖生效、留空回退默认 ✓
- 站点配置 `GET/PUT /admin/site-config` + 公开 `GET /app/site-config`：写入后公开端即时反映 ✓
- 静态站点：`https://onnsa.cn/aisock-admin/` = 200、`https://onnsa.cn/aisock/` = 200

### 微信登录说明（方案 A）
- 微信登录仅取 openid 身份，**手机号统一走阿里云短信验证码**（与微信认证无关）。
- 后端已具备 `getPhoneNumber` 一键授权换手机号能力（access_token 缓存、账号合并），**待小程序完成认证后**将登录按钮改回 `open-type="getPhoneNumber"` 即可启用，无需改后端。
- `WX_SECRET` 已确认为正确 32 位值 `e46136fd747cb2e09706f1d1c8e3dfc6`。

### 安全待办（上线前必处理）
- `SMS_MASTER_CODE=888888` 万能验证码后门：当前应客户要求保留，**正式上线前务必从服务器 `.env` 删除**，否则任意手机号可用 888888 登录。

---

## 九、多端数据同步说明
- **小程序与 Web 连同一后端、同一数据库**（基址均 `https://onnsa.cn/aisock-api`），数据按 `user.id` 归属。
- **数据互通的前提是「同一用户身份」**：
  - 两端都用**同一手机号短信登录** → 同一 `user.id`，设计/订单/AI 配额完全互通。✅
  - Web 短信账号 与 小程序**微信(openid)登录** → 默认是两个 `user.id`，数据各自独立。
- **账号合并保险（已实现）**：
  - 微信登录拿到真实手机号时（小程序认证后启用 `getPhoneNumber`）→ 自动按手机号合并到已有账号。
  - 当前方案 A 下，用户先微信登录、再用同手机号短信登录时 → 后端自动把「无手机号的 openid 临时账号」的设计/订单/花型/AI 任务迁移到手机号账号并补绑 openid，最后删除临时账号（事务保证一致性）。
- **共享内容**：袜型/花型/Banner/资讯/AI 模型配置/站点配置等来自同一后端，两端天然一致。
- **Web 端微信登录**：支持 **PC 扫码登录**（方案：小程序码 + 轮询）。Web 生成小程序码 → 微信扫码打开小程序确认页 → 已登录用户确认 → Web 轮询拿 token。复用现有小程序、无需开放平台网站应用资质；PC 直接复用小程序 `user.id`，**多端账号天然统一**。前置：`getUnlimited` 生成小程序码要求小程序**已发布**；未发布/未配密钥时退回手机号登录。

## 十、小程序上传前 Checklist

代码侧（已核对 OK）：
- manifest `mp-weixin.appid = wx3a64370afc288cc4`；无本地调试地址残留（基址为 HTTPS 线上）；最新代码已 `npm run build:mp-weixin`。

微信公众平台后台需配置：
- **服务器域名**（开发设置 → 服务器域名）：
  - request / uploadFile 合法域名：`https://onnsa.cn`
  - downloadFile 合法域名：`https://onnsa.cn`、`https://cdn.onnsa.cn`
- 业务域名：如需小程序内打开分享 H5（`onnsa.cn/aisock`）按需配 web-view 业务域名。

上传步骤：
1. 开发者工具导入 `miniprogram/packages/app/dist/build/mp-weixin`，填 AppID。
2. 编译预览，确认登录/设计/下单/AI 生图正常。
3. 上传 → 公众平台「版本管理」提交审核 → 通过后发布。

上线前另需：微信支付私钥 `apiclient_key.pem` 上传 + 商户号绑定；小程序认证后启用 `getPhoneNumber`；删除 `SMS_MASTER_CODE`；维护 KIE AI 余额。

---

## 十一、业务逻辑一致性审查与修复（2026-05-31）

对小程序 / Web 两端的「AI 设计」编辑器做了深度审查，已修复的关键问题：

| 问题 | 端 | 修复 |
|------|----|------|
| 价格前端自算、与服务端价目表差约 4 倍 | Web | 改用服务端 `/orders/quote` 权威试算；本地价目表同步服务端口径 |
| 下单把中文 label 当 value 传，服务端计价回退默认、工艺加价失效 | Web | 改传 `materialValue/craftValue`（key） |
| 保存设计只存 name+cover，丢 regions → 无法再编辑 | Web | 补 `engine/designSnapshot` 全量快照；支持 `?design=ID` 还原继续编辑 |
| 订单不关联 designId | Web | 下单前自动保存并带 designId |
| 我的设计封面不显示、无继续编辑入口 | Web | 修复封面 img 渲染 + 「继续编辑」入口 |
| canvas 临时路径直接当 coverUrl 存库会失效 | 小程序 | 保存/下单前 `uploadFile` 换永久 URL |
| 分享 inviteBonus 可无限刷 AI 额度 | 两端/后端 | 后端加每日发放上限（防刷） |

> 设计取舍（保留）：Web 用真实像素蒙版渲染引擎、小程序用 canvas 矢量渲染，两套引擎并存。

### 深层差异补齐（2026-05-31 续）
| 差异 | 端 | 处理 |
|------|----|------|
| 袜型不影响渲染 | 小程序 | 新增 `sockGeometry` 按袜型（中筒/船袜/长筒/短袜）参数化轮廓与分区，选不同袜型画面真实变化（纯几何，无需素材） |
| 袜型共用 default 素材 | Web | `buildSockResources` 改为按袜型子目录 `image-tool/{type}/` 加载，缺失回退 default → 后续补各袜型专属蒙版图即自动差异化，渲染零改动 |
| "推荐色"色卡是死功能（只高亮不映射） | 小程序 | 新增 `usePalette` 色卡→四区配色映射，点击一键套用整体配色；手动改色自动脱离色卡 |
| 色卡映射 | Web | 本就是真实逐像素映射，无需改 |

> 仍待素材：Web 各袜型专属蒙版 PNG（`image-tool/ankle|tube|short/`）由设计提供后即生效；当前 4 型在 web 端回退 default 形状、在小程序端已按几何区分。

---

## 十二、安全加固审查（2026-05-31，已修复并部署）

第二轮后端深度审查发现并修复的安全问题：

| 级别 | 问题 | 修复 | 线上验证 |
|------|------|------|----------|
| 🔴 资损 | `/pay/notify` 明文分支无验签，可伪造回调把订单刷成已支付 | 生产+真实支付能力下拒绝明文回调；回调金额与下单金额比对 | — |
| 🔴 越权 | 前缀白名单不分 HTTP 方法，`POST/DELETE /patterns/mine`、`/config/*` 未登录可访问（user_id=0 越权落库/删除） | 前缀白名单仅对 GET 放行，写操作强制登录 | 未登录 `POST /patterns/mine` → 401 ✓；公共 `GET /patterns` → 200 ✓ |
| 🟠 刷量 | `/ai/invite-bonus` 可自助调用、分享按钮每次 +3 次额度 | 移除该端点与两端分享自助加额度；邀励只经真实邀请关系发放 | `/ai/invite-bonus` → 401（端点移除）✓ |
| 🟠 体验 | AI 生成失败仍扣配额 | 失败自动退还配额 | — |
| 🟠 数据 | 管理员可跳过支付直接改订单为已发货 | 订单状态机加合法流转校验 | — |
| 🟠 数据 | 账号合并漏迁 upload/invitation | 补迁移，invitation 被邀请人侧处理唯一约束冲突 | — |

> 仍建议（需微信支付真实凭证后验证）：补微信支付 V3 平台证书验签与回调时间戳防重放（当前生产已拒绝未签名明文回调，AEAD 解密分支已具备；平台证书验签为进一步加固项）。

### 收尾修复（2026-05-31，已部署）
| 问题 | 修复 |
|------|------|
| SVG 上传存储型 XSS | 上传白名单移除 SVG，仅允许位图 + PDF |
| 支付双写不一致风险 | `handlePaidNotify` 的 payment+order 改同一事务 |
| 重复 prepay 产生多条流水 | `createPrepay` 复用订单未支付的 payment 记录 |
| 用户被禁用旧 token 仍可用 | 鉴权 cache-miss 路径校验 status，禁用即删会话踢下线（≤30s 生效） |
| access_token 缓存击穿 | `getAccessToken` 加进程内并发锁，复用在途请求 |

> 保留设计：7 天滑动会话（活跃用户自动续期）为 UX 取舍；SSRF（AI 出图转存）因 URL 来自可信 KIE 提供方，风险可接受。

---

## 十三、未完成功能补齐（2026-05-31，已部署）

第三轮"代码层面未完成项"排查后补齐：

| 端 | 原缺口 | 补齐 |
|----|--------|------|
| 小程序 | 分享只有 UI，朋友圈/群聊/好友卡片均占位，邀励链路断在前端 | `useShare` 注册 `onShareAppMessage/onShareTimeline`；`useInvite` 捕获并随登录上传 inviterId，打通后端邀请奖励；ShareSheet 渠道交互改真实（好友直发 / 朋友圈引导胶囊菜单 / 复制链接） |
| Web | 订单只有列表，无详情、无物流、无去支付（后端接口齐全但 UI 没接） | 新增 `OrderDetail` 页（状态进度/尺码/物流轨迹/联系客服）；购物车卡片可进详情 + 待支付「去支付」（`useOrderPay`） |
| Web | 首页「资讯查看更多/常见问题查看详情」死链；「最近活动」是伪造数据 | 死链接 Feed；移除伪造活动，改真实「最近设计」（可点继续编辑） |
| Web | `aiApi.derive/family`、`StyleVariant` 死导出 | 清理（web 款式衍生走本地引擎） |

> 仍待外部条件：① 客服电话 `SUPPORT_PHONE='400-000-0000'` 为占位，上线前替换真实号；② 朋友圈分享受微信限制需用户从胶囊菜单触发（已做引导），好友卡片分享已真实可用；③ 邀请奖励完整生效需小程序发布后真机验证分享链路。

---

## 十四、对照合同补齐缺失功能（2026-05-31，已部署）

逐条对照合同需求清单后，发现 5 项「合同要求但代码缺失」的功能并全部补齐（三端 + 后台）。所有改动遵循组件化 / 模块化 / 工程化：AI 能力抽 composable、UI 拆独立组件，避免单文件膨胀。

### 缺口与补齐对照

| # | 合同需求 | 端 | 补齐方案 |
|---|----------|----|----------|
| 1 | 设计导出 PNG/JPG | 小程序 + Web | 小程序：`SockCanvas.exportImage(fileType)` 支持 png/jpg；新增 `useExport` 组合式（授权 + 保存相册）；editor 加「⬇ 导出」选格式。Web：`SockCanvas.download(format)`、ParamsPanel 加导出按钮、Editor `onExport` 接线 |
| 2 | 意图分析（优化模糊指令） | 后端 + 小程序 + Web | 后端新增 `aiText.service.optimizePrompt`（DeepSeek，8s 超时，失败回退原文）+ `POST /ai/optimize-prompt`。小程序 `onAiGen` 生成前弹优化版供用户确认；Web 新增 `useAiDesign` + `AiAssetTab.vue`，优化后弹 BaseModal 供「用优化版/用原文」 |
| 3 | 底色修改（AI 花型背景改色） | 小程序 + Web | img2img：以当前印花图为参考 + 指令生成。小程序 editor「🎨 指令改色/改背景」；Web `AiAssetTab` 改色区（缩略图 + 指令输入），走 `aiApi.remix` |
| 4 | 订单可编辑 / 上传文件 / 备注 | 三端 + 后台 | 后端：`updateOrder` 加状态守卫（仅 pending/paid 可改）+ 地址；新增 `order_attachment` 表与 `listAttachments/addAttachment/removeAttachment` + `GET/POST/DELETE /orders/:id/attachments`。小程序 `OrderAttachments` 组件 + order-detail 备注/地址编辑。Web `OrderAttachments` 组件 + OrderDetail 编辑区。后台订单详情展示用户补传附件 |
| 5 | 密码登录（设置密码 + 密码登录） | 后端 + 小程序 + Web | 后端 `user.password`（bcrypt）migration；`setPassword/hasPassword/passwordLogin` + `POST /auth/password-login`（白名单）+ `PUT /user/password`。小程序登录页加「密码登录」tab + `set-password` 页 + 「我的」入口。Web 登录页加「密码」tab + Mine「账号安全」卡 + `PasswordModal` 组件 |

### 新增 / 关键文件
- 后端：`migrations/004_password_and_attachment.sql`、`services/aiText.service.ts`、`services/auth.service.ts`（密码）、`services/order.service.ts`（附件 + 状态守卫）、`routes/app/{auth,user,orders,ai}.ts`、`routes/admin/orders.ts`、`middleware/auth.ts`（password-login 白名单）
- Web：`composables/useAiDesign.ts`、`components/editor/AiAssetTab.vue`、`components/order/OrderAttachments.vue`、`components/user/PasswordModal.vue`、`api/index.ts`（uploadApi/passwordLogin/setPassword/订单附件）
- 小程序：`composables/useExport.ts`、`components/order/OrderAttachments.vue`、`pages/set-password/index.vue`、`composition/useUserStore`（loginByPassword）、service `auth/user/order` 扩展
- 后台：`api/orders.ts`（OrderAttachment）、`views/orders/index.vue`（附件展示）

### 数据库变更
- `user` 加 `password VARCHAR(128)`（bcrypt 哈希，NULL=未设密码）
- 新表 `order_attachment`（一单多附件：name/url/mime/size，按 order_id 索引）
- 已在线上执行 migration 004（`password` 列与 `order_attachment` 表均确认存在）

### 线上验证记录（2026-05-31）
- 服务器 `tsc --noEmit` 通过；四端本地 type-check 全通过（server/web/admin/miniprogram）
- `POST /auth/password-login`（未设密码用户）→「手机号或密码错误」✓（端点生效，非 404）
- `pm2 restart aisock-server` 后 `GET /api/health` = 200
- web=200 / admin=200（静态已重新部署到 `/var/www/aisock`、`/var/www/aisock-admin`）

### 设计说明 / 待办
- AI 改色（img2img）参考图需为可访问 URL：AI 生成图 / 上传图为服务端 URL，data URL 场景由 provider 优雅回退占位图（未配置/违规时不阻断）。
- 意图分析：已配 DeepSeek `deepseek-v4-flash`（推理模型）并线上验证生效（`AI_TEXT_API_URL=https://api.deepseek.com` / `AI_TEXT_MODEL=deepseek-v4-flash`）。该模型先产 reasoning 再产正式输出，服务端 `max_tokens=1024` 留足预算 + `finish_reason=length` 截断回退 + 输出去引号/限长清洗；未配置/失败/超时均回退原文，不阻断生图。实测「想要好看的花 / 樱花粉色 / 卡通小猫 / 圣诞节」均返回高质量提示词，单次约 2s。
- 订单编辑/附件仅在「待付款/已付款」可改，进入生产后锁定（前后端一致校验）。
- 小程序新增页面/相册权限需用微信开发者工具重新 `build:mp-weixin` 后上传提审才在线上生效（`manifest` 已加 `scope.writePhotosAlbum`）。

---

## 十五、第四轮深度审计与修复（2026-06-13，三端全量）

对小程序 / 后台 / 服务端做了四路并行深度审计（AI 功能、业务流程、后台契约、服务端安全），共发现并修复 2 个 P0、12 个 P1、约 20 个 P2。三端 type-check 全部通过。

### P0（资损 / 主流程死路）
| 问题 | 修复 |
|------|------|
| 生产环境手机号/密码登录用户（无 openid）支付死路：prepay 静默降级 `dev_` mock → mock-paid 被生产 403 拒绝，订单永久卡待支付 | 服务端：具备真实支付能力且无 openid 时 prepay 返回 428；新增 `POST /auth/bind-wechat`（uni.login code 换 openid 绑定，openid 临时账号自动合并）。客户端 `usePayment` 捕获 428 → 静默绑定 → 重试 prepay，打通真实支付 |
| 购买页展示阶梯价 ¥998/768/598，服务端实收 ¥28-44，价差 30 余倍 | 购买页移除假阶梯价，改为 `POST /orders/quote` 服务端实时计价（单价/数量/合计明细卡），与下单实收一致；quote/pricing 加入访客白名单（纯计算无副作用） |

### P1（功能断裂）
- **小程序 AI**：流式客户端补 HTTP 错误体识别（401 → 引导登录并写回跳，不再被伪装成本地脚本回复）；空闲超时联动 abort 释放 isProcessing（修「重试」死路）；AI 生图/改色 `status=failed` 显式 toast 且不关面板；ChatInput 实现 disabled 契约（回复中不丢用户输入）。
- **订单**：列表/详情 LEFT JOIN design 带回 `cover_url`（修「无预览图」）；新增用户侧 `POST /orders/:id/cancel`（仅 pending）+ 列表/详情取消入口；订单号撞唯一键自动重试。
- **登录回跳**：`LOGIN_RETURN_TO` 写入侧补齐（http 401 / 购买页 / 上传页拦截），登录后 reLaunch 回原页面；401 时 `uni.$emit('auth:expired')` 同步清 Pinia 登录态（修幽灵登录态）。
- **后台**：登录失败 401 不再触发「登出+reload」级联（排除登录接口 + 无 token 跳过 + 防重入）；订单详情联表用户（修「-（-）」）；「小程序配置」页过滤掉 `ai_generation`/`site_config` 结构化配置（服务端 listConfigs 白名单 + 前端 Array 过滤双保险，修一键停用 AI 配置事故面）；登录页删除默认口令展示与预填。

### P2（安全加固 / 体验）
- **服务端**：admin/app token 双向隔离（admin token 不可冒用 app 接口）；禁用 admin 账号踢下线；admin 登录按 IP+用户名限流（10 次/10 分钟）；`/ai/chat`(20/分)、`/ai/asr`(10/分)、`/ai/optimize-prompt`(10/分) 按用户限频（新增 `utils/rate-limit.ts`）；aiChat 上游超时改「空闲超时」且客户端断开即 abort 上游（省 token）；SSE 补 `no-transform`；ASR 加 30s 超时；`normalizeCover` 拒绝 wxfile:// 等本地临时路径；AI 配置/站点配置「空串不覆盖内置默认」（读写两侧）；`/admin/ai-config` 限 admin 角色（含明文 apiKey）；AI 对话提示词禁 markdown。
- **小程序**：simulateStream 480ms 定时器可取消 + stop 不再挂起 Promise；abort 语义独立（不触发兜底文案）；风格确认/跳过防抖；空推荐不出空卡；PaymentSheet 试算失败禁付+重试（修 ¥0 可支付）；支付轮询仅认 paid 及后续状态（订单被取消不再误报成功）；订单详情材质/工艺 key→文案映射；订单 tab 补全（待生产/已发货）；feed 首载失败自动重试；home 下拉刷新收回动画；上传失败清掉本地预览（防 wxfile:// 落库）；AI 占位 apiKey 脱敏。
- **后台**：全部 CRUD 弹窗改 `:on-before-ok`（校验失败/接口报错不关窗不丢表单）；keep-alive 下列表页 `onActivated` 刷新（修跨页操作后数据陈旧）；AI 监控补 pending 筛选；仪表盘 5 卡等宽一行。

### 客户真机反馈核对（客户真机反馈.txt）
四项反馈在当前代码均已落实：AI 气泡左右分边 ✓、风格单选 ✓、详情页「立即购买→购买页 / 定制设计→袜版定制页」✓、二级页返回按钮（NavBar show-back / 系统导航）✓。需重新打包小程序上传体验版给客户复验。

### 部署提示
- 后端：同步 `server/src/`（middleware/auth.ts、routes/app/{ai,auth,orders,pay}.ts、routes/admin/{auth,orders,ai-config}.ts、services/{aiChat,aiConfig,asr,auth,config,design,order,siteConfig}.service.ts、utils/rate-limit.ts 新增）→ `pm2 restart aisock-server`。
- 后台：`VITE_BASE=/aisock-admin/ npm run build` 重新部署。
- 小程序：`npm run build:mp-weixin` 重新上传（含客户反馈项复验）。

---

## 十六、体验优化与性能深度优化（2026-06-13，小程序）

### 视觉 / 交互
- **首页底部轮播晕染特效**：中心卡切换时图像从中心向四周「逐步晕染直到卡片边缘」——双层暖金涟漪波前（错峰扩散）+ 中心微光绽放 + 封面呼吸缩放回正，自然舒适（`components/home/ShowcaseCarousel.vue`）。
- **AI 录音界面**：新增 `components/ai/VoiceRecordOverlay.vue` 敦煌主题全屏浮层——声纹法相同心涟漪 + 暖金核心呼吸 + 声波律动 + 计时 + **上滑取消**（取消区转朱砂 ✕）。`useVoiceInput` 增 `elapsed/willCancel/move`，ChatInput / AiGenerateSheet 接入。
- **AI 对话等待 loading**：typing 指示器改敦煌墨点律动（沙金→棕矿物渐变 + 跃动），AI 头像思考时泛暖金光晕脉动（`pages/ai/components/ChatMessage.vue`）。
- **诗意启动页**：新增 `components/LaunchSplash.vue` 敦煌晨光主题（莲花九瓣印玺 + 浮金尘 + 晨光天幕 + 诗句「一缕花线起，脚尖生诗意」），冷启动仅首次、自动淡出（首页模块级标记控制，tab 切换不重现）。

### 性能（页面加载 / 跳转深度优化）
- **分包加载**：tab 页（home/feed/ai/cart/mine）+ login 留主包（6 页）；editor/detail/purchase/upload/designs/orders/order-detail/assets/set-password 移入子包 `pkg/`（9 页），主包体积显著下降、冷启动更快。
- **preloadRule**：进入 home/feed/ai 时预载 `pkg` 子包，首次跳转无下载等待。
- **lazyCodeLoading=requiredComponents**（manifest mp-weixin）：按需注入页面组件，开页提速。
- **列表图片 lazy-load**：feed 商品网格 / designs / assets。
- 所有 `/pages/{moved}/index` 路径引用同步改 `/pkg/`（含 domain/catalog、service/http 等），`npm run build:mp-weixin` 通过，产物分包结构经校验正确（主包 6 / 子包 pkg 9 / preloadRule / lazyCodeLoading 均生效）。

### 客户真机反馈（客户真机反馈.txt）核对 — 全部已落实
1. AI 助手对话框左右分边（AI 左、用户右）✓；袜子样式单选 ✓；样式选好后「袜版选择」按钮进袜版定制页（upload）✓。
2. 浏览详情「立即购买」→ 购买页（purchaseRoute）✓；「定制设计」→ 袜版定制页 ✓。
3. 二级页返回按钮：detail/purchase/upload 用 NavBar show-back，其余默认导航自带 ✓。

### 本轮部署记录（2026-06-13，已执行并验证）
- 后端：`scp server/src → /root/work/aisock/server/src`（含第四轮 bind-wechat / 428 支付 / 取消订单 / rate-limit / token 隔离 等），服务器 `tsc --noEmit` 通过、`pm2 restart aisock-server`、health=200；新端点实测 pricing=200 / quote=200(计价正确) / bind-wechat=401(路由存在)。
- 后台：`VITE_BASE=/aisock-admin/ npm run build` → dist 部署到 `/var/www/aisock-admin/`，`https://onnsa.cn/aisock-admin/`=200。
- 线上健康检查全 200：api/home、admin、web、health。
- **小程序**：本地已 `npm run build:mp-weixin`（dist/build/mp-weixin 含分包）。**上传/提审需微信开发者工具登录后手动执行**（导入 `miniprogram/packages/app/dist/build/mp-weixin`，AppID `wx3a64370afc288cc4` → 上传 → 提审 → 发布）。
