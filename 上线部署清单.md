# 爱花型 AI 袜版设计系统 · 上线部署清单

> 交付前照此清单逐项执行/勾选。
> 线上：https://onnsa.cn ｜ 服务器：`root@47.96.2.138`（别名 `ssh onnsa`）｜ 代码目录：`/root/work/aisock`
> 后台：https://onnsa.cn/aisock-admin/ （账号 `admin / admin123`）

---

## 一、后端代码同步（scp + 重启）

本轮累积改动的后端源码，scp 到服务器对应路径后 `pm2 restart aisock-server`：

```
server/src/middleware/auth.ts
server/src/services/ai.service.ts
server/src/services/asr.service.ts
server/src/services/aiConfig.service.ts
server/src/services/aiText.service.ts
server/src/services/article.service.ts
server/src/services/shipment.service.ts
server/src/services/pattern.service.ts
server/src/routes/app/ai.ts
server/src/routes/app/upload.ts
server/src/routes/app/user.ts
server/src/routes/admin/articles.ts
server/src/routes/admin/shipments.ts
server/src/routes/admin/patterns.ts
server/src/routes/admin/users.ts
server/.env.example
```

重启后验证：
```bash
ssh onnsa "cd /root/work/aisock/server && npx tsc --noEmit && pm2 restart aisock-server && sleep 2 && curl -s -o /dev/null -w 'health=%{http_code}\n' http://127.0.0.1:8199/api/health"
```

- [ ] 后端文件已同步
- [ ] `tsc` 通过、`pm2 restart` 完成、health=200
- [ ] 验证 `POST /api/v1/app/ai/asr` 不再 404（语音识别路由已上线）

## 二、服务器环境变量（`/root/work/aisock/server/.env`）

补 DashScope（语音/千问/万相）：
```bash
ssh onnsa "grep -q '^DASHSCOPE_API_KEY=' /root/work/aisock/server/.env || echo 'DASHSCOPE_API_KEY=sk-5bf2c2cde80642bb9b9262ef9ac60638' >> /root/work/aisock/server/.env"
ssh onnsa "grep -q '^DASHSCOPE_BASE_URL=' /root/work/aisock/server/.env || echo 'DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com' >> /root/work/aisock/server/.env"
```
- [ ] DASHSCOPE_API_KEY / DASHSCOPE_BASE_URL 已写入并重启

## 三、演示数据（一次性执行，幂等可重复）

```bash
mysql -uroot -p aisock < /root/work/aisock/server/migrations/005_seed_demo_data.sql
```
填充：袜型5 / 花型分类6 + 花型10 / Banner3 / 资讯8 / 演示用户2 / 演示订单4 + 物流2。
（不触碰首页主题/案例/AI 推荐官界面等按 UI 设计稿渲染的配置）
- [ ] 005 种子已执行（小程序浏览页、后台各列表已有数据）

## 四、运营后台前端

```bash
# 本地：cd admin ; VITE_BASE=/aisock-admin/ npm run build
# 上传 admin/dist 覆盖到服务器 /var/www/aisock-admin/
```
- [ ] admin/dist 已重新构建并部署（含 P1/P2/P3 后台修复）

## 五、微信小程序

```bash
# 本地：cd miniprogram/packages/app ; npm run build:mp-weixin
```
开发者工具导入 `dist/build/mp-weixin`（AppID `wx3a64370afc288cc4`）→ 上传 → 提审 → 发布。
- [ ] 小程序已重新打包、上传、提审

---

## 六、微信公众平台配置

### 6.1 服务器域名（开发管理 → 开发设置 → 服务器域名）
| 类别 | 域名 |
|---|---|
| request 合法域名 | `https://onnsa.cn` |
| uploadFile 合法域名 | `https://onnsa.cn` |
| downloadFile 合法域名 | `https://cdn.onnsa.cn`、`https://onnsa.cn` |
- [ ] 三类域名已配置

### 6.2 用户隐私保护指引（设置 → 服务内容声明）
声明信息类型 + 用途（详见 `小程序隐私权限与用途说明.md`）：
- [ ] 麦克风（语音输入）
- [ ] 相册（仅写入）（导出设计图）
- [ ] 选中的照片或视频信息（上传花型/拍照）
- [ ] 手机号（登录）
- [ ] 地址（下单配送）

---

## 七、登录 / 支付 凭证

### 7.1 手机号登录（阿里云短信）— ✅ 已配齐且线上验证
`SMS_PROVIDER=aliyun` / SIGN_NAME=杭州昂撒科技 / TEMPLATE_CODE=SMS_323320055，无需额外操作。

### 7.2 微信授权登录（openid）— ✅ 已配齐且线上验证
`WX_APPID=wx3a64370afc288cc4` / `WX_SECRET=e46136fd…`。
- 可选：小程序**完成认证后**，把登录按钮改回 `open-type="getPhoneNumber"` 启用微信一键手机号（后端已就绪，无需改后端）。

### 7.3 微信支付（V3）— ⚠️ 配置齐，差私钥与绑定
密钥已配：MCHID `1717685486` / SERIAL / APIV3_KEY / NOTIFY_URL。当前为 **mock 演示态**（订单真实落库，不真实扣款）。**真实收款需 2 步**：
- [ ] 微信商户平台下载商户 API 私钥 `apiclient_key.pem`，上传到服务器 `/root/work/aisock/server/certs/`，然后 `pm2 restart aisock-server`（`canRealPay()` 自动转真实支付）
- [ ] 微信商户平台把商户号 `1717685486` 与小程序 AppID `wx3a64370afc288cc4` 完成关联绑定

---

## 八、上线前安全收尾（务必！）

- [ ] **删除万能验证码**：
```bash
ssh onnsa "sed -i '/^SMS_MASTER_CODE=/d' /root/work/aisock/server/.env && pm2 restart aisock-server"
```
否则任意手机号可用 `888888` 登录。

---

## 九、AI 模型确认（百炼账号）

模型名后台「AI 配置」可改，端点固定。上线前确认客户百炼账号已开通：
- [ ] 语音 ASR `qwen3-asr-flash`（**无降级，必须确认或改成账号实际有的型号**）
- [ ] 文生图 `wan2.7-t2i-preview` / 图生图 `wan2.7-image-edit`（失败会自动降级 KIE，不阻断）
- [ ] 文本 `qwen3.7-max`（失败回退原文，不阻断）

> 说明：图片生成已做"万相失败自动降级 KIE"保险；语音 ASR 是唯一硬依赖 DashScope 的功能。

---

## 十、交付物文档

- `项目状态与部署文档.md` — 项目总览与历史
- `小程序隐私权限与用途说明.md` — 隐私指引文案
- `上线部署清单.md` — 本文件
