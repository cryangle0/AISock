# 爱花型 AI · 小程序隐私权限与用途说明

> 用于微信公众平台「用户隐私保护指引」配置，以及客户/审核备查。
> 开发者主体：杭州昂撒信息科技有限公司
> 最后更新：2026-06-12

---

## 一、收集/使用的信息类型与用途（公众平台「用户隐私保护指引」逐项填写）

在公众平台 → 设置 → 服务内容声明 → 用户隐私保护指引，按下表勾选信息类型并填写「用途」。

| 信息类型 | 是否勾选 | 对应小程序接口/场景 | 用途说明（填入"用途是"一栏） |
|----------|---------|--------------------|------------------------------|
| 麦克风 | ✅ | `getRecorderManager`（按住说话语音输入） | 将你的语音转写为文字，用于描述设计需求、检索与生成袜款花型 |
| 相册（仅写入）权限 | ✅ | `saveImageToPhotosAlbum`（导出设计图） | 将你设计或生成的袜款图片保存到你的相册 |
| 选中的照片或视频信息 | ✅ | `chooseImage`（上传花型/素材库/订单补传附件） | 用于上传你的图片作为袜款花型素材进行定制设计 |
| 手机号 | ✅ | 短信验证码登录；后续微信一键 `getPhoneNumber` | 用于账号注册登录、身份识别及订单与服务通知 |
| 地址 | ✅ | 下单填写收货地址 | 用于袜款定制商品订单的配送收货 |

> 其余信息类型（位置、摄像头、蓝牙、身份证、车牌、各类传感器、剪切板等）小程序均未使用，**不勾选**。

---

## 二、对应的小程序权限声明（已在代码 `manifest.json` 配置）

| scope | 说明文案（弹窗提示） | 触发场景 |
|-------|---------------------|----------|
| `scope.record` | 用于语音输入：把你说的话转成文字描述 | 按住说话 |
| `scope.writePhotosAlbum` | 用于将你设计的袜版导出保存到相册 | 导出设计图 |

> 注：选中照片（`chooseImage`）由系统选择器触发，无需单独 scope，但需在隐私指引中声明「选中的照片或视频信息」。
> 麦克风、相册的"运行时授权弹窗"由微信在首次使用时自动弹出，用户点「允许」即可，无需额外后台配置。

---

## 三、用途文案（可直接复制填写）

1. 访问你的麦克风，用途是：
   ```
   将你的语音转写为文字，用于描述设计需求、检索与生成袜款花型
   ```

2. 使用你的相册（仅写入）权限，用途是：
   ```
   将你设计或生成的袜款图片保存到你的相册
   ```

3. 收集你选中的照片或视频信息，用途是：
   ```
   用于上传你的图片作为袜款花型素材进行定制设计
   ```

4. 收集你的手机号，用途是：
   ```
   用于账号注册登录、身份识别及订单与服务通知
   ```

5. 收集你的地址，用途是：
   ```
   用于袜款定制商品订单的配送收货
   ```

---

## 四、配套需在公众平台配置的「服务器域名」（开发设置 → 服务器域名）

| 类别 | 域名 | 用途 |
|------|------|------|
| request 合法域名 | `https://onnsa.cn` | 后端 API 调用 |
| uploadFile 合法域名 | `https://onnsa.cn` | 上传花型图片、录音音频、订单附件 |
| downloadFile 合法域名 | `https://onnsa.cn`、`https://cdn.onnsa.cn` | 加载 OSS（CDN）上的图片/出图 |

---

## 五、填写注意事项

- 用途必须**具体到功能**，不要写"用于提供服务""提升体验"等空泛表述，否则审核易被驳回。
- 信息类型要与小程序**实际使用的接口**一致：用了就声明、没用别勾。
- 该《用户隐私保护指引》提交生效后，真机上麦克风、选图、保存相册等能力才不会被拦截，提审也才能通过。
